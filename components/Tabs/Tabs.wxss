.tabs .tabsTitle {
  display: flex;
}
.tabs .tabsTitle .titleItem {
  display: flex;
  justify-content: center;
  align-items: center;
  flex: 1;
  padding: 15rpx 0;
}
.tabs .tabsTitle .active {
  color: var(--themeColor);
  border-bottom: 5rpx solid currentColor;
}
